#include<stdio.h>
include<conio.h>

	void main()
{
	int a,b,c;
	printf("Enter values of 3 angles");
	scanf("%d%d%d",%a,%b,%c);

	if((a==60 && b==60 && c==60)
		printf("Equivalent triangle is formed");

	else
		printf("Equivalent triangle is not formed");
	getch();
}